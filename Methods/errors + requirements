Когда скачали с репозитория:
    Открываем проект, пишем в консоль

        python -m venv .venv
        .venv\Scripts\activate
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip list --outdated
